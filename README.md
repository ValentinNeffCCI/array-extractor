# array-extractor

Extract only **the fields** you want from **objects** nested in an **array** as a **json file** and return it in a **new file**

## Requirements

- **NodeJS**
- Having a **.json** file with objects inside

## How to use it

```
node index.js path/to/my/json [...fields] destination_file.json
```

## And that's it !!!